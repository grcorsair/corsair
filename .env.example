# Anthropic API Key (Required for Agent Tests)
# Get your API key from: https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-api03-...

# AWS Configuration (Optional - for real AWS testing)
# If not set, tests use fixture data
AWS_REGION=us-west-2
AWS_PROFILE=default

# Agent Test Configuration (Optional)
# AGENT_TEST_VERBOSE=true  # Enable verbose logging in agent tests
# AGENT_TEST_MODEL=haiku   # Override model selection (haiku|sonnet)
